# Copie para .env e preencha com sua chave real:
#   cp .env.example .env
# .env está no .gitignore — nunca commite a chave real.

# Chave OpenAI (usada pelo graphify e pela app)
OPENAI_API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
